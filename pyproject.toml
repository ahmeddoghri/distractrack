[project]
name = "distractrack"
version = "0.1.0"
description = "A distractor-aware video memory that remembers motion, not just the last bright blob."
readme = "README.md"
requires-python = ">=3.9"
license = {text = "MIT"}
authors = [{name = "Ahmed Doghri"}]
keywords = ["machine-learning", "research", "vision-video", "benchmark"]

[project.urls]
Homepage = "https://adoghri.com"
Repository = "https://github.com/ahmeddoghri/distractrack"
